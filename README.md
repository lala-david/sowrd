# sowrd

새 프로젝트 준비 중.

---

이전에 이 저장소에 있던 "동행(The Gospel Road)" 게임 기획·프로토타입은
커밋 `dd5886c` 에 보존되어 있습니다.

```bash
git show dd5886c --stat          # 당시 파일 목록
git checkout dd5886c -- docs/    # 필요한 것만 되살리기
```
