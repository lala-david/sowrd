/** 지도 좌표 한 점. 투영·경로·표식이 전부 이 형태를 쓴다. */
export type Pt = [number, number]

/* 한때 이 파일에는 "장의 자리들을 감싸는 부드러운 덩어리"를 만드는 코드가 있었다.
 * 월드맵에서 경로선 대신 장의 **땅**을 그리려는 시도였고, 실패했다:
 * 예수님의 사역 길은 모든 장이 갈릴리와 예루살렘을 오가서 **장이 공간으로 갈리지 않는다.**
 * 덩어리 일곱이 서로 포개져 얼룩 하나가 됐다.
 * 지금 월드맵은 아무것도 감싸지 않고 인장만 놓는다(QuestMap.tsx 머리말 참조). */
