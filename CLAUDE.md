# THE WAY — 작업 규칙

## 대원칙: 딱딱한 것을 없애고, 간단하게

이 저장소의 모든 UI·디자인 작업은 이 문장으로 먼저 판정한다.

게임화 재작업 뒤 홈 한 화면에 `자리 2/33` · `이정표 14/142` · `1장·부르심` · `VIA CHRISTI` ·
진행바 · `37km 남음` 배지가 동시에 있었다. 숫자와 라벨이 많을수록 화면은 **계기판**이 되고,
이 제품이 되려는 것(순례·묵상)과 정확히 반대 방향으로 간다.

지켜야 할 것:

1. **더하기 전에 뺄 것을 먼저 찾는다.** 카운터·라벨·배지·테두리는 기본적으로 의심한다.
2. **같은 정보를 두 번 말하지 않는다.** 진행바 + `n/N` + 퍼센트가 함께 있으면 둘은 잉여다.
3. **각진 것보다 부드러운 것.** 딱딱한 링·테두리 대신 여백·그림자·종이 결로 경계를 만든다.
   양피지의 결을 기본값으로 삼는다.
4. **한 화면에 주장 하나.** 나머지는 조용히 뒤로 물러난다.
5. 러닝 화면은 예외다 — 달리는 중에는 거대한 숫자 하나가 정답이다.

## 함께 지키는 것

- **디자인 판단은 측정으로 확인한다.** 색을 만졌으면 `node scripts/check-contrast.mjs`.
  자리 콘텐츠·여정 데이터를 만졌으면 `node scripts/check-content.mjs`(스키마·금지어·걸음 규칙).
  화면 인상을 바꿨으면 스크린샷의 평균 채도·명도 폭·찬 색 비율을 전후로 비교한다.
  실제로 지도를 양피지색으로 바꿨을 때 명도 폭이 22→9로 **떨어졌다**(개선인 줄 알았는데 악화).
- **신학 금지선은 취향이 아니다.** `docs/DECISIONS.md`와 `docs/PLANNING.md` §4.3이 정본:
  운동량↔구원·믿음 연결 금지 · 믿음점수/구원레벨 금지 · 성경에 없는 예수 말씀 창작 금지 ·
  **십자가=보스전 금지**(수난 자리는 mood로 축하가 꺼진다) · 헌금↔영적보상 금지.
- **어휘는 순례, 메커니즘은 게임**(DECISIONS D3). "레벨·클리어·보스·가챠"는 금지어.
  자리·인장·봉인·여권으로 쓴다.
- **거리가 성경을 여는 열쇠가 되면 안 된다.** 성경 본문은 도달 여부와 무관하게 항상 열린다.
- **실제 지도(러닝 경로)는 MapLibre + OpenFreeMap + 우리 양피지 스타일**(`src/lib/mapStyle.ts`,
  DECISIONS D10). OSM 공식 raster 타일은 다시 쓰지 않는다(정책 리스크). 지도 색은 테마 무관
  고정 hex — CSS 토큰 금지. 스타일을 만졌으면 `dev/map.html`로 전후 스크린샷을 찍고
  `node scripts/check-shot.mjs`로 채도·명도 폭·찬 색 비율을 비교한다.

## 지도 = 퀘스트 보드 (2026-08-19 재작업)

지도는 **세로로 내려가는 월드**다(`src/lib/board.ts` · `src/components/QuestBoard.tsx`).
장(章)마다 recraft v4.1로 생성한 손그림 월드맵 패널(`src/assets/art/world/{journey}-{tier}.webp`,
`scripts/world-art.mjs`)이 깔리고, 그 위로 하나의 길이 모든 자리를 지난다. 실좌표 투영은 버렸다 —
선이 매듭이 됐고 장이 서로 이어지지 않았다. 지리는 패널 그림이 말하고, 순서는 길이 말한다.

지켜야 할 것:

1. **분위기 연출은 그 그림의 그 자리에만.** 배는 물에, 갈매기는 바닷가에, 별은 밤에, 연기는 시내산에.
   좌표표는 `src/data/ambient.ts`(원본 그림 비율). **패널을 다시 생성하면 이 표를 다시 본다.**
2. **자리의 보상은 말씀이다.** 닿음 = 그림이 열리고 금 인장. 본문은 닿지 않아도 늘 열린다.
   수난 자리(mood=lament)는 인장은 찍히되 빛나지 않고, 그 장의 공기는 구름 그림자뿐이다.
3. **걸음을 고른다(`src/data/geo/journeys/pace.ts`).** 진행에 쓰는 거리는 실측이 아니라 실제
   [2, 12]km로 자른 구간(첫 구간 ≤3km — 첫 러닝에 첫 인장). 실측은 `measured*`로 남겨 화면에 함께 적는다.
   축척 "모드"는 두지 않는다(검토·결정: `docs/EXPERT-REVIEW-2026-08-19.md`).
4. **폰 화면은 세로로만 움직인다.** 보드·칩·시트 어느 것도 문서를 옆으로 밀면 안 된다
   (`scrollIntoView` 대신 컨테이너 scrollLeft).
5. 개발 중 폰 프레임으로 보려면 `dev/phone.html?to=map?j=jesus` (배포물에 안 들어간다).
6. 배포: `npm run build` → `dist/`를 `gh-pages` 브랜치로 push → https://lala-david.github.io/sowrd/
