# 남은 일 (2026-07-23 기준)

전문가 검증 5종(UI/UX · 프론트엔드 · 접근성/다양성 · BI 데이터 · 게임 디자인)에서 나온 항목 중
**아직 처리하지 않은 것**만 적는다. 처리한 것은 코드 주석에 근거와 수치를 남겼다.

우선순위는 "사용자가 실제로 겪는 손해" 순이다.

---

## ~~P0 — 신학 제약 위반~~ (해소, 2026-07-24)

### 에피소드 아트의 후광·과대 인물 — 처리 완료
- **어디**: `src/assets/art/episodes/*.webp` (68장)
- **무엇이었나**: PCK 제약은 "그리스도를 인물로 직접 묘사하지 않는다. 후광 금지. 사람은
  원경의 얼굴 없는 실루엣으로만"이다. 생성 모델이 구도를 완성하려고 홀로 선 인물을
  습관적으로 넣고, 그 인물을 성인으로 해석해 머리 뒤에 밝은 원반을 그렸다.
- **어떻게 해결했나**:
  1. "사람 없는 장소" 스캐폴드로 전체 재생성 → 위반 대폭 감소(49 → 육안 확인 7장)
  2. 플래그된 35장을 **컨택트 시트로 육안 전수 검수** → 실제 위반 7장 확정
     (peter-ep07 거대 인물, paul-rhegium 삼각 후광, peter-ep04 아치 인물,
      paul-appii/paul-rome/peter-ep12/peter-ep13 중간 인물)
  3. 그 7장의 **주제문이 인물을 부르고 있었다** — `"a traveller"`(단수),
     `"a sail"`(배+사람), `"door to the street"`(거리 소실점). 주제문을 사물·부감 중심으로
     고쳐 쓰고 재생성 → 7장 모두 인물 없는 풍경·사물로 정리됨(육안 확인)
- **최종 상태**: 후광 0, 그리스도 묘사 0, 화면 지배 인물 0. 남은 작은 원경 인물
  (ep02·ep05·ep08·ep09·ep10, 전부 ~12% 이하)은 스캐폴드가 명시적으로 허용하는
  "지평선의 작은 점" 범위다.
- **검출기 주의**: `scripts/audit-art.mjs`는 오탐률이 높다(밝은 풍경+어두운 사물을 후광으로
  오인). 재생성한 깨끗한 이미지도 계속 플래그한다. **게이트로 쓰지 말 것 — 육안 검수가
  authoritative하다.** 개선하려면 연결 성분을 세로 폭 프로파일로 인물/사물 분리해야 한다.

---

## P1 — 측정·데이터

### GPS 필터 — 재생 하네스로 확장 검증(실기 로그는 여전히 이상적)
- **어디**: `src/lib/geo.ts` `watchDistance()` · 검증 `scripts/gps-replay.mjs`
- 등속 칼만 필터로 교체. 합성 직선 궤적에서 −0.1~−0.5%(예전 게이트는 빌딩사이
  10'00"/km에서 −65.8%).
- **재생 하네스 추가**(2026-07-24): 정지(신호등)·도심 곡선·터널(신호 상실)·느린 조깅이
  섞인 '지저분한' 궤적으로 확장 검증. 결과 전부 ±1.3% 이내:
  개방+신호등 −0.2% · 도심곡선+정지 −1.3% · 터널 2분 −0.7% · 느린조깅9분 −0.4%.
  터널조차 −0.7%인 이유: 등속 칼만이 마지막 속도로 관성 주행해 신호 없는 구간을 메운다.
- **남은 것**: 그래도 진짜 기기 로그는 아니다. GPS 칩의 실제 잡음 특성(다중경로 상관,
  콜드스타트 드리프트)은 재생기가 완벽히 흉내 못 낸다. `ACCEL_NOISE_MS2 = 0.5` 튜닝도
  실주행 몇 건으로 확인하는 편이 좋다. 하지만 코드 없이 할 수 있는 검증은 여기까지가 최선이다.

### ~~죽은 통계 코드~~ (해소, 2026-07-24)
- `ZONES`/`zoneOf`/`zoneSeconds`/`slowestKm`/`splits[].deltaSec`/`spreadKind`와
  `--color-zone-1..5` 토큰 10개를 전부 제거했다. 색약 검사에 걸리는 5계급 존이 사라졌으므로
  "존 기준선=오늘 평균" 지적도 함께 해소(그 지적의 대상이 바로 이 죽은 zoneOf였다).
  지도용 bandOf가 오늘 평균을 쓰는 것은 의도된 설계라 유지(오늘 런 안의 상대 비교).

### ~~이동창(`WINDOW_KM = 0.2`)이 죽어 있음~~ (해소, 2026-07-24)
- **어디**: `src/state/run.ts` `tick()`
- 예전엔 `wKm >= 0.2 || wSec >= 45`(둘 중 하나)라, 느린 러너는 0.2km를 채우기 전에 45초에
  걸려 창이 닫혔다 — 10'00"/km면 45초에 75m·45표본뿐. 잡음이 가장 큰 사람에게 표본을 가장
  적게 주는 거꾸로 된 설계였다. **거리·시간 둘 다 충족(&&) + 상한 120초**로 바꿔, 느릴수록
  창이 커진다. 검증: 8'00"→45→97표본, 10'00"→45→120표본(최대 2.7배).
- 표시 해상도: "지금 속도"를 5초 단위로 반올림(실제 불확실성 ±20~35초 대비 초 단위 표시는
  과장). 검증: 347·343초→5'45", 352→5'50", 361→6'00". (전체 평균은 안정되므로 반올림 안 함.)

---

## P1 — 게임 설계

### ~~티어 경계가 불균형하다~~ (해소, 2026-07-24)
- **어디**: `src/data/geo/journeys/*.json` 의 `tiers`
- 티어별 실제 km 격차가 **베드로 668배**(t2가 0.2km), 아브라함 37배, 출애굽 7.4배였다.
- 분산 최소화 경계를 DP로 계산해 재작성: **베드로 2.6배 · 아브라함 4.0배 · 출애굽 1.8배**.
  바울은 3.3배로 원래 정상이라 유지(1차/2차/3차/항해 canonical 구조도 의미 있음).
  아브라함은 3티어로 재조정해 **2.27배**(부르심의 먼 길 142km · 가나안으로 63km ·
  약속의 땅에 뿌리내리다 87km) — 우르→하란 하나 + 나머지 균등 2분할. 자리를 지어내지 않음.
  JourneyDetail 티어 진행바를 자리 수 → **km 비율**로 바꿔, 142km 첫 티어가 이정표를 지날
  때마다 차오른다(예전엔 하란 도달 전까지 0% 고정). 검증: 40km에서 28.2%.
- JourneyDetail 티어 헤더를 목차 → **게임 챕터**로: 도달/진행/완료 상태, 진행바, 구간 km.
  Profile 등급에도 진행바 추가.

### ~~자리에 못 닿는 러닝~~ (해소, 2026-07-24)
- 이정표 간격 2.5→2.0km, 최소 구간 문턱 4→2.5km로 조정(409 → 539개). 여정 **중** 최대
  실제 간격이 전 여정 2.1km 이하로, 3km 러너는 항상 이정표를 지난다.
- 출애굽의 "15.4% 빈손"은 완주(265km) 이후 초과 러닝이었다 — 여정 중에는 빈손이 없다.

### ~~리빌 다중 자리~~ (해소, 2026-07-24)
- 도달한 모든 자리(여정 에피소드 + 예수 코스)를 통합 Moment 페이저로. 점 인디케이터로
  넘기며 각 자리가 그림·성구/사건·묵상·기도·본문 링크를 온전히 받는다.

---

## P2 — 접근성

- ~~rem 타이포 미전환~~ (해소, 2026-07-24): 224곳 px를 rem으로 손변환하는 대신,
  **브라우저/OS 루트 글꼴 크기를 감지해 인앱 배율과 곱한 zoom을 <html>에 건다**.
  브라우저에서 글자를 키운 사람은 인앱 토글 없이도 앱 글자가 커진다. 상한 1.6(360px 레이아웃
  한계). 검증: 더크게 zoom 1.3·크게 1.15, xlarge 오버플로 0건.
- ~~`<main>` 랜드마크·스킵 링크~~ (해소): App에 단일 main#main 랜드마크 + 스킵 링크
  (첫 포커스 요소, 포커스 시 나타남, href=#main). 검증 완료.
- ~~Save-Data 미대응~~ (부분 해소): RouteMap이 saveData면 OSM 타일 대신 SVG 폴백(네트워크 0).
- ~~reduced-motion 페이드 하드컷~~ (해소, 2026-07-24): 전정기관 문제는 움직임이지 투명도가
  아니다. keyframe 애니메이션(펄스·rise·pop)만 끄고, transition-property를 opacity·색상으로
  좁혀 페이드는 매끄럽게 유지. transform 트랜지션은 즉시 끝(움직임 제거). 검증 완료.
- ~~Collection 자리 버튼 접근 이름~~ (해소): aria-label에 도달 여부 포함
  ("요단강 · 다음에 닿을 자리" / "…아직 닿지 않은 자리" / "…닿은 자리").
- ~~`Journeys` 히어로 eager 로드~~ (해소): 카드 이미지·문장 10장 전부 loading="lazy". 검증 10/10.

---

## P2 — 코드·구조

- ~~`streakDays` 잔재~~ (해소): 스토어 필드·commitRun·grace-day 로직 전부 제거.
- ~~`totalKm`와 `lifetime.km`이 두 진실~~ (해소): pilgrimTotals.totalKm을 lifetime.km으로
  통일했다(progress 합이 아니라 실제 달린 모든 거리의 단조 합).
- ~~`clearFlash` 죽은 액션~~ (해소): 제거.
- ~~`prayer.ts` 미사용~~ (해소): 품고 달리기 리스트 완성. Profile 등록·삭제(validateAlias
  강제), Setup 인라인 칩 선택, Run "○○님을 품고 달립니다". v5 마이그레이션.
- ~~`goalKm`/`goalSec` 미읽음~~ (해소): Run에 목표 게이지 추가. 진행 %, "목표 달성" 안내.
- **`Courses`는 유지**: 실제 순례길(산티아고 등) 정보 화면으로 정당하다("아직"은 정직한 안내).
  탭 하이라이트만 journeys로 고쳤다.
- ~~`RouteMap` 점쌍마다 폴리라인~~ (해소): 같은 밴드끼리 병합. 실측 19구간 → 4 폴리라인.
- ~~`RouteMap`/`RouteTrace` SVG의 CSS 변수 iOS Safari 위험~~ (코드 측 해소): Leaflet은
  cssVar()로 미리 풀고, 인라인 SVG(범례·경로선·마커)의 색을 attribute→**style CSS 속성**으로
  옮겼다(CSS 속성의 var()는 WebKit에서도 확실히 풀린다). 실기 육안 확인만 남음(Expo/Safari).

---

## 검증 도구 (이번에 만든 것)

| 스크립트 | 하는 일 |
|---|---|
| `scripts/episode-art.mjs` | 여정 68자리 아트 생성(recraft v4.1) |
| `scripts/audit-art.mjs` | 후광·과대 인물 자동 검출 (오탐 있음, 위 P0 참고) |
| `scripts/trim-frames.mjs` | 생성 아트의 액자 프레임 감지·제거 |
| `scripts/gen-milestones.mjs` | 여정 좌표 대권 보간으로 이정표 409개 생성 |
| `scripts/optimize-art.mjs` | 손실 재인코딩(q75, 최대 1280px) |
| `scripts/contact-sheet.mjs` | 아트 QA용 격자 시트 |

---

## 이번 작업에서 확인된 사실 (재발 방지용)

- **부정 목록은 그 사물을 불러온다.** `"no border"` → 테두리가 그려졌고, `"no halo"` → 후광이
  그려졌다. 무엇을 그리지 말지가 아니라 **그 자리에 무엇을 그릴지**를 지정해야 한다.
- **프롬프트에 숫자·비율을 쓰면 글자로 그려진다.** 예전에 "45%"가 그림 안에 텍스트로 나왔다.
- **recraft v3는 프롬프트 상한 1000자**라 이 스캐폴드가 들어가지 않는다. v4.1을 쓰되
  비율 크기(`1:1`)만 받고 `style` 파라미터를 거부한다.
- **`accuracy`는 "어디에 있는가"의 오차지 "얼마나 움직였는가"의 오차가 아니다.** 이것을
  변위 문턱으로 쓴 것이 GPS 거리 오차 −66%의 뿌리였다.
- **위치만 평활하면 거리가 깎인다.** 랜덤워크 칼만으로 위치를 평활한 뒤 차분으로 거리를 재면
  저주파 통과 때문에 느린 이동이 사라진다(개방하늘 10'00"/km에서 −95.7%).
  속도를 상태에 넣고 |v|·dt로 적분해야 한다.
- **Windows에서 sharp가 파일 핸들을 쥔 채 같은 경로에 쓰면 `EBUSY`**가 난다.
  원본을 `fs.readFileSync`로 먼저 메모리에 올릴 것.
- **백그라운드 생성이 끝났는지는 완료 알림으로만 판단할 것.** 로그 파일 꼬리를 보고
  완료로 오판해서, 68장 생성이 아직 도는 중에 최적화·커밋을 돌렸다. 그 결과 커밋에
  재생성본과 이전본이 섞였고 31장이 미최적화(35MB)로 들어갔다. 다음 커밋에서 바로잡았지만,
  긴 배치 작업 뒤에 후처리를 붙일 때는 반드시 완료를 확인하고 시작해야 한다.
